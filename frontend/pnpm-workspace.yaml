# sharp ships a native binary used by Next.js image optimization, so its
# install script must be allowed to run. pnpm 11 gates build scripts behind
# this explicit allowlist (the package.json "pnpm" field is no longer read).
allowBuilds:
  sharp: true
